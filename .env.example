DATABASE_URL=file:db/docker-gui.db
JWT_SECRET=change-me-in-development

DEFAULT_ADMIN_EMAIL=admin@gmail.com
DEFAULT_ADMIN_PASSWORD=Admin@123
DEFAULT_ADMIN_NAME="Super Admin"
# Set to "mock" to return synthetic system metrics data instead of reading the host
SYSTEM_METRICS_PROVIDER=system

# Uncomment and adjust if you use a non-default socket or a remote Docker host
# DOCKER_SOCKET_PATH=/var/run/docker.sock # Mac Os Path: /Users/chetan/.docker/run/docker.sock
# DOCKER_HOST=tcp://127.0.0.1:2375
# DOCKER_TLS_VERIFY=1
# DOCKER_CERT_PATH=./docker-certs
