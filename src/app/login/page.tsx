import LoginForm from "./login-form";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Sign in · Docker GUI"
};

export default function LoginPage() {
  return <LoginForm />;
}
