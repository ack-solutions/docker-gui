import FeaturesDashboard from "./features-dashboard";

export const dynamic = "force-dynamic";
export const metadata = { title: "Features · Docker GUI" };

export default function FeaturesPage() {
  return <FeaturesDashboard />;
}
