import SettingsDashboard from "./settings-dashboard";

export const dynamic = "force-dynamic";
export const metadata = { title: "Settings · Docker GUI" };

export default function SettingsPage() {
  return <SettingsDashboard />;
}
